ouro_src = [
    meson.current_source_dir() / 'ouro_transformer.cpp',
    meson.current_source_dir() / 'ouro_embedding.cpp',
]

ouro_inc = include_directories('.')

quick_dot_ai_src += ouro_src
quick_dot_ai_inc += ouro_inc
